# دليل التكامل التقني لخدمة الفوترة الوطنية (Jofotara Integration Guide)

هذا الدليل مخصص للمطورين الراغبين في فهم وتطوير نظام الربط بين تطبيق "Good Line" ونظام الفوترة الوطني الأردني (جوفوترا).

---

## 📑 جدول المحتويات
1. [نظرة عامة على النظام](#نظرة-عامة-على-النظام)
2. [المتطلبات الأساسية](#المتطلبات-الأساسية)
3. [الهندسة التقنية للربط](#الهندسة- التقنية-للربط)
4. [هوية الأطراف في الفاتورة](#هوية-الأطراف-في-الفاتورة)
5. [دليل الـ API الكامل](#دليل-الـ-api-الكامل)
6. [دورة حياة إرسال الفاتورة](#دورة-حياة-إرسال-الفاتورة)
7. [إدارة الحساب والأجهزة](#إدارة-الحساب-والأجهزة)
8. [معالجة الأخطاء](#معالجة-الأخطاء)

---

## 1. نظرة عامة على النظام <a name="نظرة-عامة-على-النظام"></a>
يقوم النظام بجلب الشحنات التي تم توصيلها من قاعدة بيانات **Firebase Firestore**، وتحويل بيانات "سعر التوصيل" إلى "عناصر فاتورة" يتم إرسالها إلى نظام **جوفوترا** للحصول على فاتورة ضريبية رسمية.

**الملفات الرئيسية في المشروع:**
- `lib/services/billing/api_service.dart`: القلب المحرك لكل عمليات الـ API.
- `lib/services/billing/user_preferences.dart`: المسؤول عن تخزين التوكن والبيانات الحساسة.
- `lib/screens/NationalBilling/NationalBillingScreen.dart`: واجهة المستخدم وعمليات المعالجة الجماعية (Batch Processing).

---

## 2. المتطلبات الأساسية <a name="المتطلبات-الأساسية"></a>
للعمل مع هذا النظام، يحتاج المطور لامتلاك البيانات التالية الخاصة بالمنشأة:
- **الرقم الضريبي (Tax Number)**: المكون من 9 خانات.
- **اسم المستخدم وكلمة المرور**: الخاصة بحساب المنشأة على جوفوترا.
- **رقم النشاط (Activity Number)**: الرقم التسلسلي للفرع أو النشاط التجاري المصرح له بإصدار الفواتير.

---

## 3. الهندسة التقنية للربط <a name="الهندسة-التقنية-للربط"></a>
بسبب قيود **CORS** في متصفحات الويب، لا يتم الاتصال مباشرة بـ API جوفوترا من التطبيق، بل نستخدم وسيطاً:
1. **الويب**: يتم استدعاء **Firebase Cloud Function** (اسم الدالة: `proxyRequest`) لتعمل كـ Proxy.
2. **الموبايل**: يتم الاتصال مباشرة بـ `https://backend.jofotara.gov.jo`.

---

## 4. هوية الأطراف في الفاتورة <a name="هوية-الأطراف-في-الفاتورة"></a>
في هذا النظام، يتم إصدار فواتير من نوع **CASH_GENERAL_TAX** (فاتورة ضريبية نقدية):
- **البائع (Seller)**: هي المنشأة (صاحبة التطبيق) ويتم تحديدها عبر `Activity Number`.
- **المشتري (Buyer)**: يتم تركه `null` في العادة لأن الفواتير نقدية عامة، أو يمكن إضافة بياناته إذا كانت الفاتورة ذمم.
- **الضريبة**: مطبقة بنسبة **16%** (General Tax - SIXTEEN).

---

## 🏢 تفاصيل الحساب الفعلي (Credentials)

هذه هي البيانات الخاصة بحسابك (أو حساب العميل) التي تم إعدادها في النظام (لمستخدم **BX Press**):

- **اسم المنشأة**: BX Press 
- **الرقم الضريبي (Tax Number)**: `178043940`
- **اسم المستخدم للـ API**: `BX`
- **كلمة المرور للـ API**: `123456789Aa@`
- **رقم النشاط (Activity Number)**: `13462377`

---

## 💡 ملاحظات تقنية هامة للمرسل إليه:

1. **الربط التجريبي vs الفعلي**: تأكد من أن الـ `API_URL` المستخدم هو رابط الإنتاج (Production) المذكور في الأعلى، حيث أن هذه البيانات حقيقية ومرتبطة بنظام جوفوترا.
2. **تغيير البيانات**: إذا رغب المستخدم في تغيير "رقم النشاط" أو "كلمة المرور"، يجب القيام بذلك أولاً من لوحة تحكم جوفوترا الرسمية ثم تحديثها في التطبيق من شاشة "الإعدادات".
3. **التوكن**: النظام يقوم بطلب التوكن باستخدام (`178043940`, `BX`, `123456789Aa@`) ويخزنه محلياً، ويتم إرفاقه في كل طلب (Request) مع رقم النشاط `13462377`.

---

## 5. دليل الـ API الكامل <a name="دليل-الـ-api-الكامل"></a>

### 🔐 نظام المصادقة (Authentication)
يستخدم النظام توكن من نوع **JWT (Bearer Token)**.

- **Endpoint**: `/users/auth/login`
- **Body**: `{ "taxNumber", "username", "password" }`
- **التجديد التلقائي**: يقوم النظام بفحص `expires_in` وتجديد التوكن تلقائياً قبل أي عملية إرسال إذا كان منتهياً.

---

### 📑 عمليات الفواتير
1. **الحصول على رقم الفاتورة التالي**:
   - **Endpoint**: `GET /sme/invoices/next-tax-number`
   - **Headers**: يجب إرسال `Activity`.

2. **إرسال الفاتورة (Submit)**:
   - **Endpoint**: `POST /sme/invoices/`
   - **Headers**: `upload-from: WEB`.

3. **تحميل PDF**:
   - **Endpoint**: `GET /sme/invoices/pdf/{invoiceId}/{invoiceNumber}`

---

## 6. دورة حياة إرسال الفاتورة <a name="دورة-حياة-إرسال-الفاتورة"></a>
عندما يختار المستخدم شحنة ويضغط "إرسال":

1. **التحقق من التوكن**: تجديد التوكن إذا كان منتهياً عبر `UserPreferences`.
2. **طلب رقم الفاتورة**: الحصول على الرقم المتسلسل القادم من API جوفوترا.
3. **بناء العناصر (Mapping)**: 
   - يتم تحويل `deliveryCost` الخاص بالشحنة إلى `unitPrice`.
   - يتم حساب الضريبة: `itemTax = unitPrice * 0.16`.
   - يتم حساب المجموع: `total = unitPrice + itemTax`.
4. **الإرسال**: يتم إرسال الكائن الكامل لنظام الفوترة.
5. **التحديث المحلي**: عند نجاح العملية، يتم تحديث المستند في **Firestore** بوضع `isSentToFaotara: true`.

---

## 7. إدارة الحساب والأجهزة <a name="إدارة-الحساب-والأجهزة"></a>
يتضمن النظام دوال كاملة لـ:
- **إدارة الأجهزة (Devices)**: إضافة وتعطيل الأجهزة المصرح لها (Endpoints: `/users/devices/`).
- **المساعدين (Sub-Admins)**: إضافة موظفين آخرين للتعامل مع النظام.
- **نظام OTP**: التحقق من العمليات عبر الرسائل النصية (Endpoints: `/users/otp/`).

---

## 8. معالجة الأخطاء <a name="معالجة-الأخطاء"></a>
- **API Error 401**: التوكن غير صالح أو منتهي الصلاحية.
- **API Error 400**: خطأ في شكل البيانات (غالباً نقص في رقم النشاط أو الرقم الضريبي).
- **HttpException**: خطأ في الاتصال بالخادم أو الـ Proxy.

---

## 💡 نصائح للمطور
- استخدم `Logger` الموجود في `ApiService` لتتبع الطلبات والاستجابات في وضع التطوير.
- تأكد أن جميع المبالغ المالية يتم إرسالها بـ **3 منازل عشرية** باستخدام `toStringAsFixed(3)`.
- رقم النشاط (`Activity Number`) هو أهم حقل؛ بدونه لن يقبل النظام أي فاتورة.

---
**تاريخ التحديث**: 2026-01-27  
**فريق التطوير**: Good Line IT Department
